// Different things that can happen:
// sign in, sign up, sign up with google
// check if email is in database, check if form is valid

const firebaseValidationReducer = (state, action) => {
    switch (action.type) {
        case () {

        }
    }
}